server:
  port: 9021
spring:
  profiles:
    active: local
  application:
    name: curator-exam-info-core
  main:
    allow-bean-definition-overriding: true
# 配置监控
management:
  endpoints:
    web:
      exposure:
        # spring boot actuator 监控所有端点
        include: '*'