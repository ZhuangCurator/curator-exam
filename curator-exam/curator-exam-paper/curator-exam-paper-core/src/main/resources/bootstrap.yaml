server:
  port: 9052
spring:
  profiles:
    active: local
  application:
    name: curator-exam-paper-core

# 配置监控
management:
  endpoints:
    web:
      exposure:
        # spring boot actuator 监控所有端点
        include: '*'